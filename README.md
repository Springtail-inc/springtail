# Springtail

## Pre-requisites:
- VCPKG
  - deps on Mac:
    - brew install pkg_config (on mac)
    - brew install autoconf
  - run vcpkg.sh
- Conan -- not currently in use (C++ package manager)
  - install through pip3 `pip3 install conan`
  - make sure conan binary is in your path
- DELETE ME
